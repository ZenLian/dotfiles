local p = require("theme.palette")

local M = {
  primary = p.base,
  on_primary = p.blue,
  surface = p.surface0,
  on_surface = p.text,
  surface_variant = p.surface2,
  on_surface_variant = p.text,
  error = p.base,
  on_error = p.red,
}

return M
