{{- if eq .osid "linux-arch" -}}

{{ $packages := list
    "curl"
    "git"
    "zsh"
    "neovim"
    "fzf"
    "ripgrep"
    "fd"
    "lf"
    "bat"
    "exa"
    "git-delta"
-}}

{{ $sudo := "sudo " -}}
{{ if eq .chezmoi.username "root" -}}
{{   $sudo = "" -}}
{{ end -}}

#!/usr/bin/env bash

{{ $sudo }}pacman -Sy --noconfirm --needed {{ $packages | join " " }}

{{- end -}}
