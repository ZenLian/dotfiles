# `sheldon` configuration file
# ----------------------------
#
# You can modify this file directly or you can use one of the following
# `sheldon` commands which are provided to assist in editing the config file:
#
# - `sheldon add` to add a new plugin to the config file
# - `sheldon edit` to open up the config file in the default editor
# - `sheldon remove` to remove a plugin from the config file
#
# See the documentation for more https://github.com/rossmacarthur/sheldon#readme

[templates]
custom = '[[ ! -f $ZDOTDIR/plugins/{{ name }}.zsh ]] || source $ZDOTDIR/plugins/{{ name }}.zsh'
rename = '[[ -f {{ dir }}/{{ name }}.zsh ]] && cp {{ dir }}/{{ name }}.zsh {{ dir }}/_{{ name }}'

[plugins]

[plugins.preinit]
local = "~/.config/zsh/preinit"

[plugins.powerlevel10k]
github = "romkatv/powerlevel10k"
apply = ["source", "custom"]

[plugins.oh-my-zsh]
github = "ohmyzsh/ohmyzsh"
use = [
    "lib/theme-and-appearance.zsh",
    "lib/completion.zsh",
    "lib/commpfix.zsh",
    "lib/clipboard.zsh",
    "lib/git.zsh",
    "lib/directories.zsh",
    "plugins/git/git.plugin.zsh",
    "plugins/colored-man-pages/colored-man-pages.plugin.zsh",
    "plugins/sudo/sudo.plugin.zsh",
    ]

[plugins.fzf]
github = "junegunn/fzf"
apply = ["custom"]

[plugins.fzf-tab]
github = "Aloxaf/fzf-tab"
apply = ["source", "custom"]

[plugins.zsh-autosuggestions]
github = "zsh-users/zsh-autosuggestions"
use = ["{{ name }}.zsh"]
apply = ["custom", "source"]

# [plugins.zsh-syntax-highlighting]
# github = "zsh-users/zsh-syntax-highlighting"

[plugins.fast-syntax-highlighting]
github = "zdharma-continuum/fast-syntax-highlighting"

[plugins.zsh-completions]
github = "zsh-users/zsh-completions"

[plugins."z.lua"]
github = "skywind3000/z.lua"
apply = ["custom", "source"]

## extra completions
[plugins.sheldon]
remote = "https://github.com/rossmacarthur/sheldon/raw/trunk/completions/sheldon.zsh"
apply = ["fpath", "rename"]

[plugins.exa]
remote = "https://github.com/ogham/exa/raw/master/completions/zsh/_exa"
apply = ["fpath"]

[plugins.fd]
remote = "https://github.com/sharkdp/fd/raw/master/contrib/completion/_fd"
apply = ["fpath"]
##

[plugins.postinit]
local = "~/.config/zsh/postinit"

